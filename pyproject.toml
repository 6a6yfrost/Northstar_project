[project]
name = "northstar-project"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask==2.3.3",
    "plotly==5.17.0",
    "pandas==2.0.3",
    "python-dotenv==1.0.0",
]

[tool.vercel]
entrypoint = "flask_app:app"